# Visual Max evidence

`asset-inventory.json` is generated by
`python3 tools/visual-max/visual_assets.py inventory`. It records the current
116 shipped runtime PNGs, 113 textures directly loaded by `ArtAssets`, the
96 directional sheets, 12 VFX sheets, 29 audio files and locked source hashes.

Bulk capture output remains in ignored `artifacts/visual-max/`. Commit a small
selected baseline and review decision here only when it is reproducible.
